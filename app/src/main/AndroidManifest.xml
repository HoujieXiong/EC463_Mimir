<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MimirDemo"
        tools:targetApi="31">
        <activity
            android:name=".CameraPermissionRequest"
            android:exported="false"
            android:label="@string/title_activity_camera_permission_request"
            android:theme="@style/Theme.MimirDemo" />
        <activity
            android:name=".CameraPreview"
            android:exported="false"
            android:label="@string/title_activity_camera_preview"
            android:theme="@style/Theme.MimirDemo" />
        <activity
            android:name=".ModeScreen"
            android:exported="false"
            android:label="@string/title_activity_mode_screen"
            android:theme="@style/Theme.MimirDemo" />
        <activity
            android:name=".CameraScreen"
            android:exported="false"
            android:label="@string/title_activity_camera_screen"
            android:theme="@style/Theme.MimirDemo" />
        <activity
            android:name=".MainMenu"
            android:exported="false"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.MimirDemo" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MimirDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>